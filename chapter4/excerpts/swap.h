/* Interface for swap.c */

#ifndef SWAP_H
    #define SWAP_H

    /* swap(v[], i, j)
    
    Swaps v[i] and v[j].
    Requires: v is an array of integers, i and j are valid indices.
    Side effects: Modifies v.
    */
    void swap(int v[], int i, int j);
#endif